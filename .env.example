VITE_CONCEPT2_CLIENT_ID=your_client_id_here
VITE_CONCEPT2_CLIENT_SECRET=your_client_secret_here

# The redirect URI is derived at runtime from window.location.origin +
# /auth/concept2/callback. Register every origin you use (localhost, LAN
# IP, tunnel, etc.) in your Concept2 app's redirect-URI list.
